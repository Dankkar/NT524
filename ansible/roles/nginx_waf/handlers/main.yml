---
- name: Reload Nginx
  ansible.builtin.command:
    cmd: docker exec waf-nginx nginx -s reload
  ignore_errors: true
  tags:
    - update_rules

- name: Restart Nginx
  ansible.builtin.command:
    cmd: docker restart waf-nginx
  ignore_errors: true
